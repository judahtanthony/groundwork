.groundwork/state.sqlite
.groundwork/state.sqlite-wal
.groundwork/state.sqlite-shm
.groundwork/runs/
.groundwork/approvals/
.groundwork/views/
.groundwork/worktrees/

# Design canvas runtime sidecar (regenerated by the prototype viewer)
docs/design/wireframes/.design-canvas.state.json

# Go build artifacts
/gw
/bin/
*.test
*.out

# Frontend dependencies are developer-only. The production dist is committed
# because it is embedded into the Go binary.
/web/node_modules/

# CodeGraph index (local code-intelligence sidecar)
.codegraph/
